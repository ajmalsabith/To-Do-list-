import { StyDirective } from './sty.directive';

describe('StyDirective', () => {
  it('should create an instance', () => {
    const directive = new StyDirective();
    expect(directive).toBeTruthy();
  });
});
